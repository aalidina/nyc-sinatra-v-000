<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h2>Create New Figure</h2>

    <form action="/figures" method="post">

      <label for="name">Figure's Name</label>
      <input type="text" name="figure[name]" id="figure_name">
      <br>

      <label for="title">Figure's Title</label>
      <% Title.all.each do |title| %>
      <%= title.name %>
      <input id="title_<%= title.id %>" type="checkbox" name="figure[title_ids][]" value="<%=title.id%>">
      <% end %>
      <br><br>

      <label for="title">Create a New Title </label>
      <input type="text" name="title[name]" id="new_title"><br>

      <label for="figure">Landmark Name</label>
      <%Landmark.all.each do |landmark|%>
      <input type="checkbox" name="figure[landmark_ids][]" value="<%=landmark.id%>" id="landmark_<%=landmark.id%>"><br>
      <%=landmark.name%>
      <%end%>
      <br>
      <label for="landmark[name]">Create a New Landmark</label>
      <input type="text" name="landmark[name]" id="new_landmark">

      <br><br>

      <input type="submit"  value="Create New Figure">

    </form>

  </body>
</html>
